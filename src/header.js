import { React, Fragment } from 'react';

function Header() {
    return (
        <Fragment>
        <header>
        <h2>Esta es el header</h2>
        </header>
        </Fragment>
    );
}
export default Header;
