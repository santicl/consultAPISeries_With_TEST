import React, { Fragment } from 'react'

function Search() {
    return (
        <Fragment>
        <input placeholder="Busca tu serie anime favoritos"/>
        <button>Buscar</button>
        </Fragment>
    );
}

export default Search;
